import { PesquisaFilmeModel } from './pesquisa-filme-model';

describe('PesquisaFilmeModel', () => {
  it('should create an instance', () => {
    expect(new PesquisaFilmeModel()).toBeTruthy();
  });
});
